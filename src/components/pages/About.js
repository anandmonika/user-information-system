import React from "react";

const About = () =>{
    return(
        <div className="container">
            <h3 className="mb-3">About Page</h3>
            <p className="lead">This is an user management system built for managing the user's information at a single place. It supports addition, deletion and edit functionality along with that you can view the user information with a single click.</p>
        </div>
    );
}

export default About;